# IPTV Player - MPV Optimized v2.0

Современный и оптимизированный IPTV плеер на базе MPV и PySide6. Отличается высокой производительностью, низким потреблением памяти и стабильной работой полноэкранного режима.

![Screenshot](ui.jpg)

## Основные возможности

### 🚀 Производительность
- Использование QNetworkAccessManager для асинхронной загрузки иконок и плейлистов (не "вешает" интерфейс)
- Оптимизация памяти с использованием `__slots__` для хранения списков каналов (эффективно даже для плейлистов на 10,000+ каналов)
- Умное кэширование логотипов каналов

### 🎬 Видео
- Ядро MPV обеспечивает поддержку практически любых форматов потоков
- Аппаратное ускорение (`hwdec=auto`) включено по умолчанию

### 🔊 Звук
- Программное усиление громкости до 130% (полезно для тихих каналов)
- Корректная синхронизация ползунка громкости при старте

### 🖥️ Интерфейс
- **Полноэкранный режим**: Корректное скрытие панелей управления и восстановление размеров окна при выходе
- **Адаптивность**: Левая панель занимает 1/4 экрана по умолчанию, но размер можно менять
- **Фильтрация**: Мгновенный поиск с задержкой ввода (debounce) для предотвращения фризов

### 📋 Управление плейлистами
- Поддержка локальных файлов и URL
- Автоматическое обновление плейлистов (если ссылка устарела более чем на 24 часа)
- Возможность принудительного обновления
## Установка и настройка

### 1. Установка Python зависимостей

Убедитесь, что у вас установлен Python 3.8 или новее.

```bash
# Создание виртуального окружения (рекомендуется)
python -m venv venv

# Windows:
venv\Scripts\activate

# Linux/macOS:
source venv/bin/activate

# Установка библиотек
pip install PySide6 python-mpv qtawesome
```

**Зависимости:**
- `PySide6`: Графический интерфейс
- `python-mpv`: Python-обертка для MPV
- `qtawesome`: (Опционально) Иконки FontAwesome. Если не установить, будут использоваться текстовые символы

### 2. Настройка MPV (Важно!)

Для работы плеера необходима библиотека libmpv. Сам по себе `pip install python-mpv` не устанавливает видео-движок.

#### Для Windows (Самый простой способ)

1. Скачайте сборку MPV для Windows (например, с shinchiro builds или bootstraps)
2. Распакуйте архив
3. Найдите файл `mpv-1.dll` (иногда называется `libmpv-2.dll` или просто `mpv.dll`)

**Вариант А (Рекомендуемый):** Создайте папку `mpv` рядом с файлом `main_mpv.py` и положите `mpv-1.dll` туда.

**Вариант Б:** Положите `mpv-1.dll` прямо рядом со скриптом `main_mpv.py`.

Структура папок должна выглядеть так:
```
project_folder/
├── main_mpv.py
├── mpv/
│   └── mpv-1.dll
└── ...
```

#### Для Windows (Через Chocolatey)

Если вы опытный пользователь, можно установить через пакетный менеджер:

```powershell
choco install mpv
```

Плеер автоматически найдет библиотеку по стандартным путям Chocolatey.

#### Для Linux / macOS

Установите системную библиотеку:
```bash
# Ubuntu/Debian:
sudo apt install libmpv-dev mpv

# Arch:
sudo pacman -S mpv

# macOS:
brew install mpv
```
## Запуск

```bash
python main_mpv.py
```

## Использование

### Управление плейлистами

1. Нажмите кнопку ➕ (Добавить) в правой панели
2. Выберите вкладку "Из интернета" (для ссылки) или "Локальный файл"
3. Нажмите ОК

**Чекбокс "Авто":**
- Если включен: При запуске программы плеер проверит, когда плейлист обновлялся последний раз. Если прошло более 24 часов, он скачает свежую версию по ссылке
- Если выключен: Всегда загружается локальная кэшированная версия (быстрый старт)

**Кнопка 🔄 (Обновить):** Принудительно скачивает плейлист по ссылке прямо сейчас

### Горячие клавиши

| Клавиша      | Действие                           |
|--------------|------------------------------------|
| F11          | Вкл/Выкл полноэкранный режим       |
| ESC          | Выход из полноэкранного режима     |
| Пробел       | Пауза / Воспроизведение            |
| Двойной клик | Вкл/Выкл полноэкранный режим (по видео) |
## Решение проблем

### ❌ Ошибка: Библиотека mpv не найдена!
Скрипт не видит `mpv-1.dll`. Убедитесь, что вы скачали библиотеку (см. пункт Установка) и положили её рядом со скриптом или в папку `mpv`.

### ❌ Ошибка: qt.network.http2: stream error / Иконки не грузятся
Плеер автоматически переключается на HTTP/1.1 для совместимости с серверами иконок, которые не поддерживают HTTP/2. Если иконки не грузятся, возможно, сервер недоступен или заблокирован в вашем регионе.

### ❌ Видео тормозит / рассыпается
В коде включено аппаратное ускорение (`hwdec='auto'`). Если у вас старая видеокарта или проблемы с драйверами, попробуйте найти в коде `MPV_SETTINGS` и заменить `'hwdec': 'auto'` на `'hwdec': 'no'`.

## Лицензия

MIT License. Свободное использование и модификация.