🍫 Установить **Chocolatey** (пакетный менеджер для Windows) на Windows 10 можно через **PowerShell**, запущенный с правами администратора.

-----

## 💻 Пошаговая установка Chocolatey через PowerShell

Следуйте этим шагам:

### 1\. Запустите PowerShell от имени администратора

  * Нажмите **Win + X** или **щелкните правой кнопкой мыши** по кнопке «Пуск».
  * В появившемся меню выберите **«Windows PowerShell (администратор)»** или **«Терминал Windows (администратор)»** (если установлен).
    > 

### 2\. Проверьте политику выполнения (Execution Policy)

Chocolatey рекомендует временно изменить политику выполнения, чтобы разрешить скрипту установки работать.

  * Введите следующую команду и нажмите **Enter**:
    ```powershell
    Get-ExecutionPolicy
    ```
  * Если результат — **`Restricted`**, вам нужно его изменить.

### 3\. Установите политику выполнения

  * Если политика ограничена, выполните одну из следующих команд. Рекомендуется использовать `Bypass -Scope Process` для временного обхода, который сработает только для текущего сеанса PowerShell:
    ```powershell
    Set-ExecutionPolicy Bypass -Scope Process -Force
    ```
    Или, если вы предпочитаете более безопасный вариант:
    ```powershell
    Set-ExecutionPolicy AllSigned
    ```

### 4\. Выполните команду установки Chocolatey

  * Вставьте следующую длинную команду в окно PowerShell и нажмите **Enter**. Эта команда загрузит и запустит установочный скрипт Chocolatey:
    ```powershell
    Set-ExecutionPolicy Bypass -Scope Process -Force; [System.Net.ServicePointManager]::SecurityProtocol = [System.Net.ServicePointManager]::SecurityProtocol -bor 3072; iwr https://community.chocolatey.org/install.ps1 -UseBasicParsing | iex
    ```
    > **Примечание:** Убедитесь, что вы скопировали всю команду целиком.

### 5\. Проверьте установку

  * После завершения установки (обычно это занимает несколько секунд) закройте и **заново откройте PowerShell** (также от имени администратора).
  * Введите команду:
    ```powershell
    choco
    ```
  * Если установка прошла успешно, вы увидите информацию о версии Chocolatey и доступных командах.

-----

## 💡 Использование Chocolatey

Теперь вы можете использовать `choco` для установки программ. Например, чтобы установить **Visual Studio Code**, вы введете:

```powershell
choco install vscode
```

  * Чтобы установить без запроса подтверждения, добавьте ключ `-y`:
    ```powershell
    choco install vscode -y
    ```
  * Для обновления всех установленных пакетов:
    ```powershell
    choco upgrade all -y
    ```
	